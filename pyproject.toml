[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aci"
version = "0.0.1"
description = ""
dependencies = []

[project.optional-dependencies]
dev = [
    "ruff ==0.15.4",
    "black ==26.1.0",
    "mypy ==1.19.1",
]
